# PDF Carver Specification

Detect header, parse bounded object/xref evidence where feasible, locate coherent EOF markers without assuming the first marker is definitive, and validate with a robust parser. Fragmented PDFs may require metadata assistance.