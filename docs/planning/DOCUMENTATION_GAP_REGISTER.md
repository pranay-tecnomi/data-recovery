# Documentation Gap Register

## Closed for MVP
Core module boundaries, state machines, error taxonomy, persistence baseline, FFI envelope, XPC protocol, corpus manifest and acceptance matrix.

## Deferred
HFS+ implementation details — post-MVP.
NTFS implementation details — later.
APFS deleted recovery — research.
Additional formats — backlog.

Deferred scope cannot silently enter MVP.